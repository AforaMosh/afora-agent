import Foundation
import AforaKit

// Prefer the AforaKit wrapper to keep gateway request payloads consistent.
typealias AnyCodable = AforaKit.AnyCodable
typealias InstanceIdentity = AforaKit.InstanceIdentity
