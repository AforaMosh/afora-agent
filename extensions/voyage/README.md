# Afora Voyage Provider

Official Afora memory embedding provider plugin for Voyage AI.

Install from Afora:

```bash
afora plugins install @afora/voyage-provider
afora gateway restart
```

Set `VOYAGE_API_KEY`, then configure memory search with `provider: "voyage"`.
See <https://docs.afora.ai/reference/memory-config> for setup and
configuration.
